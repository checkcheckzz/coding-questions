/*
Given a sorted integer array and a specific integer, find the first position of that integer. Array contains a lot of duplicates.

*/
 
/*
solution: binary search

O(logn) time, O(1) space

*/
int GetFirst(int a[], int n, int target)
{
    assert(a && n > 0);
 
    int beg = 0;
    int end = n-1;
    while (beg <= end)
    {
        int mid = beg + (end-beg)/2;
 
        if (a[mid] == target && (0 == mid || a[mid-1] != target))
            return mid;
        else if (a[mid] >= target)//note the = here
            end = mid - 1;
        else
            beg = mid + 1;
    }
 
    return -1;
}
